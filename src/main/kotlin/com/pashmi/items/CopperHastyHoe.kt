package com.pashmi.items

import net.minecraft.item.HoeItem
import net.minecraft.item.ToolMaterial

class CopperHastyHoe(
    material: ToolMaterial,
    attackDamage: Int,
    attackSpeed: Float,
    settings: Settings,
) : HoeItem(material, attackDamage, attackSpeed, settings)

